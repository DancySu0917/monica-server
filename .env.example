# ═══════════════════════════════════════════════════════════════
# Monica Medical AI Server - 环境变量模板
# 使用方式：cp .env.example .env  然后填写真实值
# 注意：.env 文件绝对不能提交到 Git！
# ═══════════════════════════════════════════════════════════════

# 生成命令：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-min-32-chars-please-change-this

# ── 微信小程序 ──────────────────────────────────────────────────
WX_APPID=wx3049de7a1babc585
WX_SECRET=ae4bd8700c5be8780e664c68f6ca7ed8

# ── 数据库 ──────────────────────────────────────────────────────
# Docker 部署时数据库文件放在 volume 中
DATABASE_URL=sqlite:////opt/monica-server/db/monica.db

# ── Redis（Docker Compose 时 host 为 service name "redis"）──────
REDIS_URL=redis://127.0.0.1:6379/0

# ── LLM 配置（单一 OpenAI 兼容后端，无降级逻辑）────────────────
# 填写你的 API base URL（支持任意 OpenAI 兼容服务）
LLM_BASE_URL=https://yundou.ai/v1
# 填写对应的 API Key
LLM_API_KEY=sk-3c27b7341a154029e21d0c67c903e795fc757c3553cb9fef276e7f7e4d81f9df
# 指定使用的模型名称（需支持多模态/视觉能力，如 gpt-4o / gemini-2.5-pro 等）
LLM_MODEL=gpt-5.4

# ── 存储 ────────────────────────────────────────────────────────
# Docker 部署时使用容器内绝对路径
STORAGE_ROOT=/opt/monica-server/storage
MAX_UPLOAD_SIZE_MB=500
CHUNK_SIZE_MB=5

# ── 配额保护 ────────────────────────────────────────────────────
DAILY_TOKEN_LIMIT=200000

# ── 性能（适配 2C2G 服务器）────────────────────────────────────
ARQ_MAX_JOBS=1
ARQ_JOB_TIMEOUT=600
DICOM_BATCH_SIZE=50
TOP_K_SLICES=10
TOTALSEG_FAST=true

# ── CORS（填写真实域名，多个用逗号分隔）────────────────────────
ALLOWED_ORIGINS=https://codebetter.cn, https://api.codebetter.cn

# ── 开发模式（⚠️ 生产环境必须为 false 或不设置）─────────────────
# 设为 true 后允许用 code="monica-code" 或 "dev_xxx" 绕过微信登录
DEV_MODE=false
